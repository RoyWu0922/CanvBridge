# CanvBridge 安装与打开步骤

下载 `CanvBridge.app`（macOS 专用，Intel 或 Apple Silicon 均可）后按以下步骤打开。

> **运行形态**：双击后 App 会**自己弹出原生窗口**（内嵌网页界面），不再占用浏览器标签页。
> 需要退回「浏览器打开」模式时，可用源码 `CANVBRIDGE_WEBVIEW=0 python run_app.py` 启动。

## 第一次打开（会被 macOS 拦一次，属正常现象）

因为 App 没有经过 Apple 公证，macOS 首次会拦：**"无法打开，因为无法验证开发者"**。放行方法（任选其一）：

**方法一：右键 → 打开**
1. 在 Finder 里**右键** `CanvBridge.app`
2. 点 **"打开"**
3. 弹窗点 **"打开"**，即可运行

**方法二：系统设置**
1. 双击 `CanvBridge.app` 被拦后
2. 打开 **系统设置 → 隐私与安全性**
3. 在页面底部点 **"仍要打开"**，再点 **"打开"**

> 之后就可以正常双击打开了，只需放行这一次。

## 使用前提

- **Mac 系统**（macOS 12 或更新）
- **已安装 Google Chrome**（AIMS 自动登录要用系统 Chrome）
- **CityU 学生账号**（AIMS EID + 密码、Canvas Token）

## 首次使用配置

1. 双击打开 App，会弹出 CanvBridge **原生窗口**（自带的本地界面）
2. **一次设置迁移**：界面从浏览器换成了 App 内窗口，两者的本地存储互不相通——首次需在新窗口「设置」里**重填一次** Canvas URL / Token、LLM Key（之后会保存在 App 窗口内，下次打开仍在）
3. 首次写日历 / 提醒 / 选文件夹时，macOS 会弹权限授权，点 **允许**
4. 外链（如点进 Canvas 公告/作业原文）会用系统默认浏览器打开

## 常见问题

**Q：双击后没有弹出原生窗口？**
可手动在浏览器访问 `http://127.0.0.1:8331` 应急；或确认是否是旧版打包。

**Q：提示"已损坏，无法打开"？**
终端执行：`xattr -cr /Applications/CanvBridge.app`，再重开。

**Q：写日历失败？**
到 系统设置 → 隐私与安全性 → 日历，勾选允许本机终端 / App 写入。

**Q：这个 App 安全吗？**
- AIMS 密码只存 **你本机 macOS 钥匙串**，不上传、不进 Git
- Canvas Token / LLM Key 只在浏览器本地保存
- 服务只监听 `127.0.0.1`（仅本机可访问）
- 代码完全开源：<https://github.com/RoyWu0922/CanvBridge>
